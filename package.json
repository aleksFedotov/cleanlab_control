{
  "name": "cleanlab-control",
  "version": "0.1.0",
  "private": true,
  "description": "CleanLab Pro — учёт стирок в прачечной. VPS: Express + SQLite (server/).",
  "scripts": {
    "test": "npm --prefix server test"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
